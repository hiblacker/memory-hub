# TODO

本文件只记录**尚未决策**或**明确延后评估**的事项，不承载 V1 已确认需求。

## 待定

### 图片 Markdown 支持

- 状态：待定（V1 不做）
- 背景：当前正文以 Markdown 文本为主；V1 明确不支持 `![alt](url)` 图片语法渲染与归档。
- 待评估问题：
  - 是否允许外链图片，还是仅允许用户上传后的本地/对象存储地址
  - 预览与思源两侧的安全策略（SSRF、热链、隐私图）
  - 是否需要附件表、容量限制与清理策略
- 决策前约束：实现与文档均按“不支持图片 Markdown”处理，遇到图片语法时提示不支持。

## 已删除且不再排期

以下事项已从需求中移除，不要重新写入 V1/后续范围，除非发起新的产品确认：

- LLM 自动润色 / 一键改写记忆正文
- 完整 WYSIWYG 富文本编辑
- 小红书式多图瀑布流、贴纸层等复杂图文排版引擎
- 思源到 MemoryHub 的正文回写编辑
- 自定义 CSS 主题市场
