# 代码贡献指南

## 当前阶段

设计方案已经确认，项目进入 V1 业务开发阶段。业务实现必须遵循 V1 范围、ADR、测试策略和安全边界，不实现明确排除的 ChatGPT 网页自动监听、后台抓取或隐藏长期记忆读取。

## 开发环境

- Node.js 22 LTS
- pnpm 10
- Docker Engine 26+ 与 Docker Compose v2
- Git 2.40+

```bash
corepack enable
pnpm install
pnpm typecheck
pnpm test
pnpm lint
```

## 分支与提交

- 分支格式：`feat/<topic>`、`fix/<topic>`、`docs/<topic>`、`chore/<topic>`。
- 提交使用 Conventional Commits，例如 `docs: clarify automatic archive rules`。
- 一个提交只处理一个可审查目标，不提交密钥、数据库文件、构建产物或个人对话样本。
- 一个独立功能块完成并通过聚焦检查后，必须立即在本地提交；不得等多个无关功能完成后再一次性提交。
- 默认不推送到远程仓库；仅在用户明确要求发布或同步时再执行 `git push`。

## 变更流程

1. 先创建或更新 issue，明确用户价值和验收标准。
2. 架构、数据模型、安全边界或依赖变化必须新增 ADR。
3. 先写测试，再写实现；修复缺陷必须包含回归测试。
4. 数据库变化必须附带迁移、回滚说明和兼容性测试。
5. API 变化必须更新共享 Zod Schema、OpenAPI 和连接器契约测试。
6. 提交前运行完整质量命令。
7. 功能块通过检查后立即创建本地 Conventional Commit。

## 功能块交付节奏

每个功能块使用以下固定节奏：

1. 明确范围和验收标准。
2. 实现代码、测试、契约和文档的必要变更。
3. 运行该功能块的聚焦检查。
4. 使用 Conventional Commits 创建独立本地提交。
5. 除非用户明确要求，否则不推送远程仓库。

## Pull Request 检查项

- [ ] 变更范围与 V1 设计一致。
- [ ] 新增或更新了单元测试。
- [ ] 未记录 Token、密码或真实对话内容。
- [ ] API 和数据迁移保持向后兼容，或明确记录破坏性变化。
- [ ] 异步失败不会阻断 ChatGPT/Claude Code 客户端。
- [ ] 自动归档路径包含脱敏、冲突检查和审计。
- [ ] 文档与部署配置已同步更新。

## 代码审查重点

- 所有外部输入必须经过 Zod 验证。
- 所有任务处理器必须幂等。
- 外部服务错误必须转换为稳定的领域错误，不回传密钥或原始响应。
- 业务状态变更必须与审计记录位于同一事务。
- 禁止在 Web 前端读取思源 Token 或模型密钥。
